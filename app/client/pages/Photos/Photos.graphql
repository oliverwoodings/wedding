query Photos ($type: PhotoType!) {
  photos(type: $type) {
    id
    name
    width
    height
    thumbnailLink
    downloadLink
    uploader {
      id
      guests {
        firstName
        lastName
      }
    }
    tags
  }
}
